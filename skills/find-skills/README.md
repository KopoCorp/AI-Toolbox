# find-skills

Vendored from the `find-skills` skill in [vercel-labs/skills](https://github.com/vercel-labs/skills). Only the standalone `SKILL.md` is kept here — the upstream repo is the full `npx skills` CLI source (TypeScript, tests, build tooling), which this skill doesn't need at runtime.

To update: re-fetch `skills/find-skills/SKILL.md` from upstream and replace this folder's `SKILL.md`.
