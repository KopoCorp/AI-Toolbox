# agent-browser

Vendored from the `agent-browser` skill in [vercel-labs/agent-browser](https://github.com/vercel-labs/agent-browser). This is a discovery stub: it points the agent at `npm i -g agent-browser` and then has the installed CLI serve its real workflow docs live (`agent-browser skills get core`), so no CLI source needs to be vendored here.

To update: re-fetch `skills/agent-browser/SKILL.md` from upstream and replace this folder's `SKILL.md`.
